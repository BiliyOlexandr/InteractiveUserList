package com.db2limited.interactiveuserlist.presentation.empty;

public interface EmptyView {
  // In this version, there are no events to which the View should respond.
  // Once they appear, put them here and implement on UI layer.
}
